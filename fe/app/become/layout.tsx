import React from "react";
import Footer from "@/components/seller/StepInfo";

export default function BecomeLayout({ children }: { children: React.ReactNode }) {
    return (
        <div>
            {children}
        </div>
    );
}